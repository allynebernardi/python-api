# python-api

Run 
``python user.py run``

    curl --location --request GET 'localhost:5000/users' \
    --header 'Content-Type: application/json' \
    --data-raw '{
        "name": "aniela",
        "email": "bla"
    }'